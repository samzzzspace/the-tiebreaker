export interface Decision {
  id: string;
  dilemma: string;
  method: string;
  analysis: string;
  createdAt: string;
}

export interface DecisionHistoryItem {
  id: string;
  dilemma: string;
  method: string;
  createdAt: string;
}
